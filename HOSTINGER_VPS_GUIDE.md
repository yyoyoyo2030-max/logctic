# دليل الرفع السهل على سيرفر Hostinger VPS

بناءً على طلبك، تم إعداد هذا المشروع ليكون جاهزاً للرفع المباشر دون الحاجة لأي تعديلات على كود المصدر (Source Code) بعد الآن. سيتم إدارة الإعدادات من خلال ملف متغيرات `.env` مخصص يتم إنشاؤه على الخادم.

---

## الخطوة الأولى: رفع الملفات إلى السيرفر

لديك طريقتان رئيسيتان:

### الطريقة (أ): عبر Git (مُوصى بها للسرعة وتسهيل التحديث)
1. قم بالدخول إلى السيرفر الخاص بك عبر SSH:
   ```bash
   ssh root@your_server_ip
   ```
2. انتقل إلى مجلد الويب الخاص بك (غالباً ما يكون `/var/www/html` إذا كنت تستخدم Apache):
   ```bash
   cd /var/www/html
   ```
3. اسحب المشروع من Github:
   ```bash
   git clone https://github.com/yyoyoyo2030-max/logctic.git .
   ```

### الطريقة (ب): النقل المباشر عبر SFTP (مثل برنامج FileZilla)
1. افتح FileZilla وسجل الدخول بمعلومات سيرفر الـ VPS الخاص بك.
2. اذهب إلى مجلد الموقع (`/var/www/html`).
3. اسحب جميع ملفات المشروع من جهازك إلى السيرفر وضعها هناك.

---

## الخطوة الثانية: إعداد المتغيرات (جدول المتغيرات)

حتى لا تحتاج لتعديل ملف `config/config.php`، تم بناء نظام يعتمد على ملف مخفي يسمى `.env`.
لقد وفرنا لك ملفاً كقالب باسم `.env.example`.

1. في مسار المشروع على السيرفر، انسخ ملف المثال لإنشاء ملف الإعدادات:
   ```bash
   cp .env.example .env
   ```
2. افتح ملف `.env` وقم بتعديل المتغيرات فيه لتتناسب مع استضافتك:
   ```bash
   nano .env
   ```

### جدول المتغيرات المتاحة في `.env`

| المتغير | الوظيفة | مثال للقيمة |
| --- | --- | --- |
| `ENVIRONMENT` | بيئة العمل (لعدم إظهار الأخطاء للمستخدمين) | `production` |
| `SITE_URL` | رابط النطاق (الدومين) الخاص بك | `http://194.xxx.xxx.xxx` أو `https://yourdomain.com` |
| `DB_HOST` | مستضيف قاعدة البيانات | `localhost` |
| `DB_USER` | اسم مستخدم قاعدة البيانات | `root` أو `db_user` |
| `DB_PASS` | كلمة مرور قاعدة البيانات | `p@ssw0rd123` |
| `DB_NAME` | اسم قاعدة البيانات التي سيتم إنشاء الجداول فيها | `logctic_db` |

احفظ الملف بعد التعديل بالضغط على `CTRL + X` ثم `Y` ثم `Enter`.

---

## الخطوة الثالثة: إنشاء جداول قاعدة البيانات تلقائياً

لقد جهزت لك ملفاً يقوم بقراءة وبناء الجداول تلقائياً من `database.sql`:

1. افتح المتصفح الخاص بك وتوجه إلى رابط موقعك متبوعاً بـ `install.php`:
   مثال:
   `http://yourdomain.com/install.php`
2. سيقوم النظام فوراً بالاتصال بقاعدة البيانات وبناء الجداول، وفي حال لم يكن هناك مشرف، سيقوم بإنشاء حساب افتراضي:
   - **اسم المستخدم:** `admin`
   - **كلمة المرور:** `admin123`

> [!WARNING]  
> **خطوة أمنية هامة جداً:** بمجرد ظهور رسالة النجاح في صفحة التثبيت، يجب عليك حذف الملف لكي لا يتم استخدامه مجدداً.
> ```bash
> rm install.php
> ```

---

## أخطاء شائعة وكيفية تجنبها

- **مشكلة الصلاحيات:** إذا واجهت مشكلة في رفع الصور أو حفظ الجلسات، تأكد من إعطاء الصلاحية لخادم الويب (مثل `www-data`):
  ```bash
  chown -R www-data:www-data /var/www/html/
  chmod -R 775 /var/www/html/uploads/
  chmod -R 775 /var/www/html/sessions/
  ```
- **غياب ملف `.env`:** إذا ظهرت رسالة خطأ بالاتصال، تأكد أنك لم تقم بإنشاء الملف باسم خاطئ، يجب أن يكون `.env` فقط بدون أي امتداد آخر.
