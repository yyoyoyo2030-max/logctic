# 🚀 دليل رفع وتشغيل النظام على استضافة Hostinger

هذا الدليل يشرح لك خطوة بخطوة كيفية أخذ هذا النظام من منصة GitHub ورفعه ليعمل بنجاح على استضافة Hostinger (أو أي استضافة cPanel مماثلة).

---

## 📦 الخطوة الأولى: تجهيز الملفات (تنزيل النظام)

1. من صفحة GitHub الخاصة بالنظام، اضغط على الزر الأخضر **"Code"** ثم اختر **"Download ZIP"**.
2. سيتم تنزيل ملف مضغوط يحتوي على جميع ملفات النظام.
3. فك الضغط عن الملف في جهازك.
4. ادخل إلى المجلد الذي تم فك الضغط عنه، وقم بتحديد جميع الملفات بداخله (وليس المجلد نفسه)، ثم قم بضغطهم مجدداً في ملف ZIP (مثلاً `logistic_system.zip`).

---

## 🗄️ الخطوة الثانية: إعداد قاعدة البيانات في Hostinger

1. قم بتسجيل الدخول إلى حسابك في **Hostinger** (لوحة تحكم hPanel).
2. من القائمة الجانبية، اذهب إلى **Databases** (قواعد البيانات) ثم اختر **Management** (إدارة).
3. قم بإنشاء قاعدة بيانات جديدة:
   - **اسم قاعدة البيانات (Database Name):** (مثال: `u123456_logctic`)
   - **اسم المستخدم (Username):** (مثال: `u123456_admin`)
   - **كلمة المرور (Password):** (اكتب كلمة مرور قوية واحفظها جيداً)
4. بعد إنشاء قاعدة البيانات، اضغط على زر **Enter phpMyAdmin** (الدخول إلى phpMyAdmin) بجانب قاعدة البيانات التي أنشأتها.
5. في phpMyAdmin، من القائمة العلوية اختر **استيراد (Import)**.
6. قم برفع ملف `sql/complete_database_setup.sql` الموجود ضمن ملفات النظام، ثم اضغط على الزر **انطلق (Go)** في الأسفل لتنفيذ الاستيراد.

---

## 📤 الخطوة الثالثة: رفع الملفات إلى الاستضافة

1. من لوحة تحكم Hostinger، اذهب إلى **Files** (الملفات) ثم اختر **File Manager** (مدير الملفات).
2. ادخل إلى المجلد الرئيسي لموقعك (عادة يكون اسمه `public_html`).
3. اضغط على أيقونة **Upload** (رفع) لرفع ملف `logistic_system.zip` الذي قمت بإنشائه في الخطوة الأولى.
4. بعد اكتمال الرفع، اضغط بزر الفأرة الأيمن على ملف الـ ZIP واختر **Extract** (استخراج).
5. (مهم) تأكد أن جميع الملفات (مثل `index.php` ومجلد `views`) موجودة مباشرة داخل `public_html` وليس داخل مجلد فرعي.

---

## ⚙️ الخطوة الرابعة: ربط النظام بقاعدة البيانات

1. من مدير الملفات في Hostinger، ابحث عن الملف المسمى `.env.example`.
2. قم بتغيير اسم هذا الملف (Rename) ليصبح `.env` فقط.
3. اضغط بزر الفأرة الأيمن على ملف `.env` واختر **Edit** (تعديل).
4. قم بتحديث بيانات الاتصال بقاعدة البيانات لتتطابق مع ما قمت بإنشائه في الخطوة الثانية:

```env
ENVIRONMENT=production
DB_HOST=localhost
DB_NAME=اسم_قاعدة_البيانات_هنا
DB_USER=اسم_المستخدم_هنا
DB_PASS=كلمة_المرور_هنا
SITE_URL=https://yourdomain.com
```

*(استبدل `https://yourdomain.com` برابط موقعك الفعلي)*
5. اضغط على **Save** لحفظ التعديلات.

---

## 🔒 الخطوة الخامسة: إعدادات هامة أخيرة

1. **إصدار PHP**: من لوحة تحكم Hostinger، اذهب إلى **Advanced** ثم **PHP Configuration**، وتأكد أن إصدار PHP محدد على `7.4` أو `8.0` أو `8.1`.
2. **الصلاحيات (Permissions)**: مجلد `uploads/` (الذي يتم فيه حفظ المرفقات) يجب أن تكون صلاحياته (CHMOD) معينة على `755` أو `777` حتى يتمكن النظام من حفظ الملفات به.

---

## 🎉 مبروك! النظام الآن يعمل

قم بزيارة رابط موقعك (مثال: `https://yourdomain.com`).
بيانات الدخول الافتراضية للمدير (إذا لم تغيرها من قاعدة البيانات):
- **اسم المستخدم:** admin
- **كلمة المرور:** admin123

⚠️ **ملاحظة أمنية هامة جداً:** يجب تغيير كلمة المرور للمدير فور تسجيل الدخول لأول مرة من خلال صفحة الملف الشخصي.
