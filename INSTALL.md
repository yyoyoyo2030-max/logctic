# دليل التثبيت السريع ⚡

## الخطوات الأساسية

### 1️⃣ تحضير البيئة
```bash
# تأكد من تشغيل XAMPP
# شغل Apache و MySQL من Control Panel
```

### 2️⃣ نسخ الملفات
```bash
# انسخ المجلد logctic إلى:
C:\xampp\htdocs\logctic
```

### 3️⃣ إنشاء قاعدة البيانات
1. افتح: `http://localhost/phpmyadmin`
2. أنشئ قاعدة بيانات: `logistic_system`
3. اختر: **utf8mb4_unicode_ci**
4. استورد: `database.sql`
5. استورد: `update_database_for_drivers.sql`

### 4️⃣ تشغيل التطبيق
افتح المتصفح:
```
http://localhost/logctic
```

## الدخول الافتراضي 🔐

### مدير النظام
```
اسم المستخدم: admin
كلمة المرور: admin123
```

### مستخدم فرع
```
اسم المستخدم: branch1
كلمة المرور: admin123
```

## استكشاف الأخطاء ❗

### خطأ في الاتصال بقاعدة البيانات
- تحقق من تشغيل MySQL
- راجع ملف: `config/config.php`
- تأكد من اسم قاعدة البيانات

### مشكلة رفع الملفات
- تحقق من وجود مجلد `uploads/`
- تأكد من صلاحيات الكتابة

### النص العربي يظهر ???
- تأكد من: `utf8mb4_unicode_ci` في phpMyAdmin
- تحقق من: `SET NAMES utf8mb4` في config.php

## الخطوات التالية 🚀

1. غير كلمات المرور الافتراضية
2. أضف الفروع من قائمة "الفروع"
3. أضف السائقين من "السائقين"
4. ابدأ برفع التحويلات

## للمزيد من المعلومات 📚
راجع ملف: `README.md`
